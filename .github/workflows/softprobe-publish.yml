# PD5.4 -- unified publish workflow.
#
# Fires on annotated semver tags: v*.*.*
# All components are published atomically in parallel.
# On failure of any component the GitHub Release is NOT created so that
# re-tagging (or re-running) can retry without partial publishes.
#
# Submodule auth: GitHub does not grant GITHUB_TOKEN permission to clone *private* sibling
# repos (there is no org toggle for that—only reusable-actions “Access” is separate).
# softprobe/thelake is public, so actions/checkout + submodules works with GITHUB_TOKEN.
# If thelake is ever private again, add a PAT secret and pass it as `token:` on checkout.
#
# ### Docker / GCP — Artifact Registry only (not GCR)
#
# Do **not** switch Docker pushes back to Container Registry (`gcr.io/...`). Legacy GCR lacks
# first-class cleanup policies; Artifact Registry is the supported host for cleanup rules,
# vulnerability scanning, and future GCP defaults.
#
# One-time **per GCP project** that pulls or pushes these images (CI publish project, Cloud Run
# deploy project, etc.): create the Docker repository, then grant the CI / deploy principal
# `roles/artifactregistry.writer` (or project-wide equivalent).
#   gcloud artifacts repositories create softprobe \
#     --repository-format=docker --location=us-central1 --project=PROJECT_ID
#
# Components & destinations:
#   softprobe-js       -> npm (@softprobe/softprobe-js)
#   softprobe-python   -> PyPI
#   softprobe-java     -> Maven Central
#   softprobe-go       -> tag submodule -> pkg.go.dev (Go module proxy)
#   softprobe-runtime  -> Artifact Registry (us-central1-docker.pkg.dev/.../softprobe/softprobe-runtime)
#   softprobe-datalake -> Artifact Registry (us-central1-docker.pkg.dev/.../softprobe/softprobe-datalake)
#   softprobe-proxy    -> GCS (WASM binary only, no Docker)
#   CLI binaries       -> GCS (linux/mac x amd64/arm64)
#   agent-skills       -> GCS (skill package + manifest + version)
#
name: softprobe publish

on:
  create:
    ref_type: tag
  workflow_dispatch:

env:
  GCS_BUCKET: softprobe-published-files
  VERSION: ${{ github.ref_name }}

# Prevent concurrent publishes of the same tag; cancel older runs if a tag
# is re-pushed.
concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'push' }}

permissions:
  contents: write   # needed for release notes and GHA release
  packages: write   # GHCR
  id-token: write   # GCS OIDC

jobs:
  # -- Validate ----------------------------------------------------------------

  validate:
    name: Validate version ${{ github.ref_name }}
    runs-on: [self-hosted, Linux]
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Debug context
        run: |
          echo "github.ref=${{ github.ref }}"
          echo "github.ref_name=${{ github.ref_name }}"
          echo "github.event_name=${{ github.event_name }}"
          echo "github.sha=${{ github.sha }}"
      - name: Validate semver
        run: |
          TAG="${{ github.ref_name }}"
          if ! echo "$TAG" | grep -qE '^v[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "ERROR: ref_name '$TAG' is not a valid semver tag (v*.*.*)"
            exit 1
          fi
          echo "[OK] $TAG is a valid semver tag"

  # -- JavaScript SDK ----------------------------------------------------------

  js-sdk:
    name: JS SDK -> npm
    needs: validate
    runs-on: [self-hosted, Linux]
    # id-token required for npm OIDC trusted publishing (self-hosted: unset tokens)
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Install Git
        run: sudo apt-get update && sudo apt-get install -y git
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: "22.14.0"
          cache: "npm"
          cache-dependency-path: softprobe-js/package-lock.json
      - name: Ensure npm supports trusted publishing
        run: npm install -g npm@11.5.1
      - name: Inject version
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          python3 -c "
          import json, pathlib, os
          v = os.environ.get('VERSION')
          p = pathlib.Path('softprobe-js/package.json')
          d = json.loads(p.read_text())
          d['version'] = v
          p.write_text(json.dumps(d, indent=2) + '\n')
          print('version set to', v)
          "
      - name: Install + build
        run: cd softprobe-js && npm ci && npm run build
      - name: Dry-run package
        run: cd softprobe-js && npm pack --dry-run
      - name: Publish to npm (OIDC)
        run: |
          cd softprobe-js
          unset NODE_AUTH_TOKEN
          unset NPM_TOKEN
          npm publish --access public --registry=https://registry.npmjs.org --no-provenance
      - name: Print workflow identity
        run: |
          echo "repository=${{ github.repository }}"
          echo "ref=${{ github.ref }}"
          node -v
          npm -v

  # -- Python SDK -------------------------------------------------------------

  python-sdk:
    name: Python SDK -> PyPI
    needs: validate
    runs-on: [self-hosted, Linux]
    permissions:
      id-token: write
    steps:
      - name: Install Git
        run: sudo apt-get update && sudo apt-get install -y git
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Inject version
        working-directory: softprobe-python
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          python3 -c "
          import re, pathlib, os
          v = os.environ.get('VERSION')
          p = pathlib.Path('pyproject.toml')
          text = p.read_text()
          text = re.sub(r'^version = \"[^\"]*\"', f'version = \"{v}\"', text, flags=re.MULTILINE)
          p.write_text(text)
          print('version set to', v)
          "
      - name: Install build tools
        working-directory: softprobe-python
        run: pip install build twine
      - name: Build
        working-directory: softprobe-python
        run: python -m build
      - name: Publish to PyPI
        working-directory: softprobe-python
        run: twine upload dist/*
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}

  # -- Java SDK ----------------------------------------------------------------

  java-sdk:
    name: Java SDK -> Maven Central
    needs: validate
    runs-on: [self-hosted, Linux]
    permissions:
      contents: read
    steps:
      - name: Install Git
        run: sudo apt-get update && sudo apt-get install -y git
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          server-id: ossrh
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
      - name: Inject version (strip v prefix for Maven)
        working-directory: softprobe-java
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          mvn versions:set -DnewVersion="$VERSION" -DgenerateBackupPoms=false
      - name: Publish to Maven Central
        working-directory: softprobe-java
        run: mvn --no-transfer-progress -P release deploy -DskipTests
        env:
          MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.OSSRH_TOKEN }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}

  # -- Go SDK -----------------------------------------------------------------

  go-sdk:
    name: Go SDK -> pkg.go.dev
    needs: validate
    runs-on: [self-hosted, Linux]
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      - name: Update softprobe-go to latest main
        run: |
          cd softprobe-go
          git fetch origin main
          git checkout main
          echo "[OK] softprobe-go at $(git log --oneline -1 origin/main)"
      - name: Tag and push softprobe-go
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          cd softprobe-go
          git config user.email "ci@softprobe.dev"
          git config user.name "Softprobe CI"
          git tag -d "$VERSION" 2>/dev/null || true
          git push origin :refs/tags/"$VERSION" 2>/dev/null || true
          git tag -a "$VERSION" -m "Release $VERSION"
          git push origin "$VERSION"
          echo "[OK] softprobe-go tagged at $VERSION"
      - name: Verify module is accessible
        run: |
          sleep 5
          curl -sf "https://proxy.golang.org/github.com/softprobe/softprobe-go/@v/${{ github.ref_name }}.mod" | head -1
          echo "[OK] Go module published"

  # -- Runtime Docker image ----------------------------------------------------
  # Pushes to Artifact Registry — see file header; do not revert to gcr.io.

  runtime-image:
    name: Runtime -> Artifact Registry
    needs: validate
    runs-on: [self-hosted, Linux]
    permissions:
      contents: read
      id-token: write
    env:
      # Must match `gcloud artifacts repositories create ... --location=` (see file header).
      AR_LOCATION: us-central1
      RUNTIME_IMAGE: us-central1-docker.pkg.dev/cs-poc-sasxbttlzroculpau4u6e2l/softprobe/softprobe-runtime
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCS_SERVICE_ACCOUNT_KEY }}
      # Registry hostname is REGION-docker.pkg.dev (not gcr.io).
      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.AR_LOCATION }}-docker.pkg.dev --quiet
      - name: Docker metadata (Artifact Registry)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.RUNTIME_IMAGE }}
          tags: |
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v') }}
            type=ref,event=tag
      - name: Build and push (Artifact Registry)
        uses: docker/build-push-action@v6
        with:
          context: ./softprobe-runtime
          file: ./softprobe-runtime/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ github.ref_name }}
      - name: Smoke pushed image (main -> latest)
        if: github.ref == 'refs/heads/main'
        run: docker run --rm ${{ env.RUNTIME_IMAGE }}:latest --version
      - name: Smoke pushed image (release tag)
        if: startsWith(github.ref, 'refs/tags/v')
        run: docker run --rm ${{ env.RUNTIME_IMAGE }}:${{ github.ref_name }} --version

  # -- Datalake Docker image ---------------------------------------------------
  # Pushes to Artifact Registry — see file header; do not revert to gcr.io.

  datalake-image:
    name: Datalake -> Artifact Registry
    needs: validate
    runs-on: [self-hosted, Linux]
    permissions:
      contents: read
      id-token: write
    env:
      AR_LOCATION: us-central1
      DATALAKE_IMAGE: us-central1-docker.pkg.dev/cs-poc-sasxbttlzroculpau4u6e2l/softprobe/softprobe-datalake
    steps:
      # datalake/ is a private submodule; relative URL in .gitmodules + token so clone works in CI.
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Verify datalake Dockerfile
        run: test -f datalake/Dockerfile
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCS_SERVICE_ACCOUNT_KEY }}
      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.AR_LOCATION }}-docker.pkg.dev --quiet
      - name: Docker metadata (Artifact Registry)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DATALAKE_IMAGE }}
          tags: |
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v') }}
            type=ref,event=tag
      - name: Build and push (Artifact Registry)
        uses: docker/build-push-action@v6
        with:
          context: ./datalake
          file: ./datalake/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # -- Proxy WASM -> GCS -------------------------------------------------------

  proxy-wasm:
    name: Proxy WASM -> GCS
    needs: validate
    runs-on: [self-hosted, Linux]
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      # https://github.com/marketplace/actions/rust-cache — toolchain-aware keys + pruned target cache
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: softprobe-proxy -> target
          # Self-hosted runners: avoid stripping ~/.cargo/bin across runs (see action README)
          cache-bin: "false"
      - name: Install build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler file
      - name: Build WASM
        working-directory: softprobe-proxy
        run: |
          make check-deps-build
          make build
      - name: WASM SHA256
        id: wasmhash
        working-directory: softprobe-proxy
        run: |
          echo "sha=$(cat target/wasm32-unknown-unknown/release/sp_istio_agent.wasm.sha256)" >> "$GITHUB_OUTPUT"
      - name: Authenticate to GCS
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCS_SERVICE_ACCOUNT_KEY }}
      - name: Upload WASM to GCS
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: softprobe-proxy/target/wasm32-unknown-unknown/release/sp_istio_agent.wasm
          destination: softprobe-published-files/agent/proxy-wasm/${{ github.ref_name }}
          headers: |-
            content-type: application/wasm
      - name: Upload SHA256 to GCS
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: softprobe-proxy/target/wasm32-unknown-unknown/release/sp_istio_agent.wasm.sha256
          destination: softprobe-published-files/agent/proxy-wasm/${{ github.ref_name }}
          headers: |-
            content-type: text/plain
      - name: Write version file
        run: |
          echo "${{ github.ref_name }}" > version
      - name: Upload version to GCS
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: version
          destination: softprobe-published-files/agent/proxy-wasm
          headers: |-
            content-type: text/plain
      - name: Smoke GCS URL
        run: |
          URL="https://storage.googleapis.com/softprobe-published-files/agent/proxy-wasm/${{ github.ref_name }}/sp_istio_agent.wasm"
          curl -sfI "$URL" | grep -i "content-type: application/wasm"
          echo "GCS WASM URL: $URL"

  # -- CLI binaries -> GCS -----------------------------------------------------

  cli:
    name: CLI -> GCS
    needs: validate
    runs-on: [self-hosted, Linux]
    strategy:
      matrix:
        goos: [linux, darwin]
        goarch: [amd64, arm64]
    env:
      VERSION: ${{ github.ref_name }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: softprobe-runtime/go.mod
          cache-dependency-path: softprobe-runtime/go.sum
      - name: Build
        working-directory: softprobe-runtime
        env:
          CGO_ENABLED: "0"
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          echo "${{ github.ref_name }}" > ../version
          go build -trimpath \
            -ldflags "-s -w -X softprobe-runtime/internal/version.Version=${{ github.ref_name }}" \
            -o "../dist/softprobe-${{ matrix.goos }}-${{ matrix.goarch }}" \
            ./cmd/softprobe
      - name: Authenticate to GCS
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCS_SERVICE_ACCOUNT_KEY }}
      - name: Upload version file
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: version
          destination: softprobe-published-files/cli/softprobe
          headers: |-
            content-type: text/plain
      - name: Upload to GCS
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: dist/softprobe-${{ matrix.goos }}-${{ matrix.goarch }}
          destination: softprobe-published-files/cli/softprobe/${{ github.ref_name }}
          headers: |-
            content-type: application/octet-stream
            content-disposition: attachment
      - name: Smoke
        run: |
          URL="https://storage.googleapis.com/softprobe-published-files/cli/softprobe/${{ github.ref_name }}/softprobe-${{ matrix.goos }}-${{ matrix.goarch }}"
          curl -sfI "$URL" | grep -i "content-type"
          echo "GCS URL: $URL"

  # -- Agent skills -> GCS ------------------------------------------------------

  agent-skills:
    name: Agent Skills -> GCS
    needs: validate
    runs-on: [self-hosted, Linux]
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Validate ai-context.md (local)
        run: python3 scripts/validate-ai-context.py
      - name: Verify production ai-context.md
        run: |
          URL="https://docs.softprobe.dev/ai-context.md"
          for i in $(seq 1 18); do
            if curl -sf "$URL" | grep -q "Last updated:"; then
              echo "OK: $URL"
              exit 0
            fi
            echo "attempt $i: waiting for docs host..."
            sleep 10
          done
          echo "error: $URL unreachable or invalid; deploy docs first" >&2
          exit 1
      - name: Package skill
        run: |
          python3 scripts/package-agent-skill.py softprobe-test-writer \
            --version "${{ github.ref_name }}"
      - name: Authenticate to GCS
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCS_SERVICE_ACCOUNT_KEY }}
      - name: Upload expanded skill to GCS
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: dist/agent-skills/softprobe-test-writer
          destination: softprobe-published-files/agent/skills/${{ github.ref_name }}
      - name: Upload skill archive to GCS
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: dist/softprobe-test-writer.zip
          destination: softprobe-published-files/agent/skills/${{ github.ref_name }}
          headers: |-
            content-type: application/zip
            content-disposition: attachment
      - name: Upload manifest to GCS
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: dist/manifest.json
          destination: softprobe-published-files/agent/skills/${{ github.ref_name }}
          headers: |-
            content-type: application/json
      - name: Upload version to GCS
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: dist/version
          destination: softprobe-published-files/agent/skills/softprobe-test-writer
          headers: |-
            content-type: text/plain
      - name: Smoke GCS URLs
        run: |
          BASE="https://storage.googleapis.com/softprobe-published-files/agent/skills/${{ github.ref_name }}"
          curl -sfI "$BASE/softprobe-test-writer.zip" | grep -i "content-type: application/zip"
          curl -sf "$BASE/softprobe-test-writer/SKILL.md" | grep "name: softprobe-test-writer"
          echo "GCS skill URL: $BASE/softprobe-test-writer.zip"

  # -- GitHub Release ---------------------------------------------------------

  release:
    name: Create GitHub Release
    needs: [js-sdk, python-sdk, java-sdk, go-sdk, runtime-image, datalake-image, proxy-wasm, cli, agent-skills]
    runs-on: [self-hosted, Linux]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Generate release notes
        env:
          VERSION: ${{ github.ref_name }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Collect all GCS download URLs
          CLI_URLS=""
          for PLATFORM in linux darwin; do
            for ARCH in amd64 arm64; do
              URL="https://storage.googleapis.com/softprobe-published-files/cli/softprobe/${VERSION}/softprobe-${PLATFORM}-${ARCH}"
              CLI_URLS="${CLI_URLS}\n  - \`softprobe-${PLATFORM}-${ARCH}\`: ${URL}"
            done
          done

          WASM_URL="https://storage.googleapis.com/softprobe-published-files/agent/proxy-wasm/${VERSION}/sp_istio_agent.wasm"
          WASM_SHA256_URL="https://storage.googleapis.com/softprobe-published-files/agent/proxy-wasm/${VERSION}/sp_istio_agent.wasm.sha256"

          SKILL_URL="https://storage.googleapis.com/softprobe-published-files/agent/skills/${VERSION}/softprobe-test-writer.zip"

          cat > release_notes.md << EOF
          # Softprobe $VERSION

          ## Downloads

          ### CLI
          ${CLI_URLS}
          Run: \`curl -sfL https://storage.googleapis.com/softprobe-published-files/cli/softprobe/${VERSION}/softprobe-linux-amd64 | bash\`

          ### Container Images (Artifact Registry — not gcr.io)
          - Runtime: \`us-central1-docker.pkg.dev/cs-poc-sasxbttlzroculpau4u6e2l/softprobe/softprobe-runtime:${VERSION}\`
          - Runtime (moving tag): \`us-central1-docker.pkg.dev/cs-poc-sasxbttlzroculpau4u6e2l/softprobe/softprobe-runtime:latest\`
          - Datalake: \`us-central1-docker.pkg.dev/cs-poc-sasxbttlzroculpau4u6e2l/softprobe/softprobe-datalake:${VERSION}\`
          - Datalake (moving tag): \`us-central1-docker.pkg.dev/cs-poc-sasxbttlzroculpau4u6e2l/softprobe/softprobe-datalake:latest\`

          ### Proxy WASM
          - Binary: ${WASM_URL}
          - SHA256: ${WASM_SHA256_URL}

          ### SDKs
          - **JS**: \`npm install @softprobe/softprobe-js@${VERSION}\`
          - **Python**: \`pip install softprobe==${VERSION}\`
          - **Java**: Maven Central (\`com.softprobe:softprobe-java:${VERSION}\`)
          - **Go**: \`go get github.com/softprobe/softprobe-go@${VERSION}\`

          ### Agent Skills
          - **softprobe-test-writer**: ${SKILL_URL}
          EOF
          echo "release_notes.md:"
          cat release_notes.md
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Softprobe ${{ github.ref_name }}
          body_path: release_notes.md
          draft: false
          prerelease: false